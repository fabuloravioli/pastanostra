<form method="post" action="{{ path('app_pasta_delete', {'id': pasta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pasta.id) }}">
    <button class="btn">Delete</button>
</form>
