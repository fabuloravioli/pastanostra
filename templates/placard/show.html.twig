{% extends 'base.html.twig' %}

{% block title %}Placard{% endblock %}

{% block body %}
    <h1>Placard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ placard.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ placard.description }}</td>
            </tr>
            <tr>
                <th>Publiée</th>
                <td>{{ placard.publiée ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_placard_index') }}">back to list</a>

    <a href="{{ path('app_placard_edit', {'id': placard.id}) }}">edit</a>

    {{ include('placard/_delete_form.html.twig') }}
{% endblock %}
