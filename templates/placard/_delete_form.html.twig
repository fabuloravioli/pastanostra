<form method="post" action="{{ path('app_placard_delete', {'id': placard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ placard.id) }}">
    <button class="btn">Delete</button>
</form>
