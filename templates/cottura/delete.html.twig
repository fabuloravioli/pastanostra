<form method="post" action="{{ path('app_cottura_delete', {'id': cottura.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cottura.id) }}">
    <button class="btn">Delete</button>
</form>
